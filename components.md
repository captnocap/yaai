
## chat components
chat container:
{header:(prompt, model, title, settings), body:(scroll container:[message container:(message header:[model avatar, model name, timestamp], message body:[message text], message actions:[Copy, Regenerate, Like, Save to Memory, Delete, Branch, Export])]), input container:(input header: [attachments?, multi-select?, prompt context?], input body:[text area], input footer:[attach button, tools, mcp, token estimator (message), token count (entire chat), send button])}
